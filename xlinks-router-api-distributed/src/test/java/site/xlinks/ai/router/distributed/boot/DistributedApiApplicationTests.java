package site.xlinks.ai.router.distributed.boot;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import site.xlinks.ai.router.distributed.DistributedApiApplication;

@SpringBootTest(classes = DistributedApiApplication.class)
class DistributedApiApplicationTests {

    @Test
    void contextLoads() {
    }
}
